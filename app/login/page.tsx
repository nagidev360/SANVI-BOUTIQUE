'use client'
import Link from 'next/link'
import {useState} from 'react'
import {createClient} from '@/lib/supabase/client'
export default function Login(){const [signup,setSignup]=useState(false);const [loading,setLoading]=useState(false);const [msg,setMsg]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setMsg('');const f=new FormData(e.currentTarget);const supabase=createClient();const email=String(f.get('email'));const password=String(f.get('password'));const result=signup?await supabase.auth.signUp({email,password}):await supabase.auth.signInWithPassword({email,password});if(result.error)setMsg(result.error.message);else if(signup)setMsg('Account created. Check your email if confirmation is enabled.');else window.location.href='/account';setLoading(false)}return <main><header><Link className="brand" href="/">SANVI <span>BOUTIQUE</span></Link><nav><Link href="/shop">Shop</Link></nav></header><section className="cart-page"><div className="checkout-form" style={{maxWidth:520,margin:'auto'}}><p className="eyebrow">SANVI ACCOUNT</p><h1 style={{fontSize:52}}>{signup?'Create Account':'Welcome Back'}</h1>{msg&&<p>{msg}</p>}<input name="email" type="email" placeholder="Email Address" required form="authform"/><input name="password" type="password" placeholder="Password" minLength={6} required form="authform"/><form id="authform" onSubmit={submit}></form><button className="cta" form="authform" disabled={loading}>{loading?'Please wait...':signup?'Create Account':'Login'}</button><button type="button" onClick={()=>setSignup(!signup)} style={{background:'transparent',color:'#211b19'}}>{signup?'Already have an account? Login':'New customer? Create account'}</button></div></section></main>}
